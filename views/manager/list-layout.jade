//-
   Created by pedro on 09/04/15.

extends manager-layout.jade

include manager-mixins.jade

block content
    +contentIntro (intro.title, intro.description)
    .container
        .col-lg-12
            +listPagination(managerModule, page, pages, pageSize, availablePageSizes, true)
            +listAdd ('/' + managerModule + '/add')
        .col-lg-12
            block table
            +listPagination(managerModule, page, pages, pageSize, availablePageSizes, false)
    +listScripts(managerModule)